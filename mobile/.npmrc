# expo-router traz suporte a web (@expo/ui/vaul/@radix-ui) que pina peer
# deps de react-dom incompatíveis com o resto da árvore — sem isso, todo
# npm install trava em ERESOLVE.
legacy-peer-deps=true
